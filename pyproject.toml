[project]
name = "remote-mcp"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
# Add FastAPI and Uvicorn for a lightweight authenticated HTTP proxy
dependencies = ["fastmcp", "fastapi[standard]"]
